`/codex:result <job-id>` on a job just observed running answers `No job found
for "<job-id>". Run /codex:status to list known jobs.` while `/codex:status`
lists that exact id as RUNNING; omitting the id works.

Fix the underlying defect in this repository. Run the relevant existing tests
to check your work.
